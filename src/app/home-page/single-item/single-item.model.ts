export class SingleItem {
    id: number;
    title: string;
    comment: string

    constructor(id: number, title: string, comment: string) {
        this.id = id;
        this.title = title;
        this.comment = comment;
    }
}